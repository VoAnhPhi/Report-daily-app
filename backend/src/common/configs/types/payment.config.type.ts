export type PaymentConfigType = {
  pay2s: {
    accessKey: string;
    secretKey: string;
    partnerCode: string;
    apiUrl: string;
    redirectUrl: string;
    ipnUrl: string;
    accountNumber: string;
    bankId: string;
  };
};
